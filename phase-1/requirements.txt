# Phase 1 — Security Layer (KMS · OIDC · Kafka · OPA)
# Install: pip install -r requirements.txt

# Crypto (KMS key hierarchy)
cryptography>=42.0

# API framework
fastapi>=0.111
starlette>=0.37
uvicorn>=0.29

# JWT / OIDC
python-jose[cryptography]>=3.3

# HTTP client (OPA client + tests)
httpx>=0.27

# Testing
pytest>=8.0
pytest-cov>=4.0
pytest-asyncio>=0.23
