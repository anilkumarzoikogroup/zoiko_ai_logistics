# Phase 2 — Service Pipeline
# Install: pip install -r requirements.txt
# Note: also install phase-0 and phase-1 requirements first

# Database
psycopg2-binary>=2.9

# API framework
fastapi>=0.111
uvicorn>=0.29
pydantic>=2.0

# HTTP client (TestClient in tests)
httpx>=0.27

# Redis (idempotency store)
redis>=5.0

# Testing
pytest>=8.0
pytest-cov>=4.0
pytest-anyio>=0.0.0
anyio>=4.0
