# Zoiko AI Logistics — SC-001 Combined Requirements
# Covers Phase 0, 1, 2, and 3
# Install once from zoiko-logistics/ root:
#   pip install -r requirements.txt
#   pip install -e phase-0/packages/zoiko-common
#   pip install -e phase-1/packages/zoiko-kms

# ── Cryptography ──────────────────────────────────────────────────────────────
cryptography>=42.0
pycose>=1.1
python-jose[cryptography]>=3.3

# ── Database ──────────────────────────────────────────────────────────────────
psycopg2-binary>=2.9
alembic>=1.13
sqlalchemy>=2.0

# ── API Framework ─────────────────────────────────────────────────────────────
fastapi>=0.111
starlette>=0.37
uvicorn>=0.29
pydantic>=2.0
python-multipart>=0.0.9     # FastAPI file upload (parse_invoice_file endpoint)

# ── HTTP Client ───────────────────────────────────────────────────────────────
httpx>=0.27

# ── Messaging ─────────────────────────────────────────────────────────────────
redis>=5.0                  # idempotency cache + token CONSUMED lock
kafka-python>=2.0           # real Kafka in staging/prod (MockKafkaBroker in dev)

# ── Observability ─────────────────────────────────────────────────────────────
opentelemetry-api>=1.24
opentelemetry-sdk>=1.24
opentelemetry-exporter-otlp>=1.24
structlog>=24.0

# ── Dashboard (Phase 0) ───────────────────────────────────────────────────────
streamlit>=1.35
pandas>=2.0

# ── GCP (prod only — comment out for pure local dev) ─────────────────────────
# google-cloud-kms>=2.22

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0
pytest-cov>=5.0
pytest-asyncio>=0.23
anyio>=4.0
httpx>=0.27                 # FastAPI TestClient async support
