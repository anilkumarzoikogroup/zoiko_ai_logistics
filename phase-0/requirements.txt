# Phase 0 — Cryptographic Foundation
# Install: pip install -r requirements.txt

# Core crypto
cryptography>=42.0
pycose>=1.1

# Database
psycopg2-binary>=2.9
alembic>=1.13
sqlalchemy>=2.0

# Redis (idempotency store)
redis>=5.0

# Observability
opentelemetry-api>=1.24
opentelemetry-sdk>=1.24
opentelemetry-exporter-otlp>=1.24
structlog>=24.0

# Auth / JWT
python-jose[cryptography]>=3.3

# GCP (prod only — comment out for pure local dev)
google-cloud-kms>=2.22

# HTTP client (used by OPA client and tests)
httpx>=0.27

# Dashboard
streamlit>=1.35
pandas>=2.0

# Testing
pytest>=8.0
pytest-cov>=5.0
pytest-asyncio>=0.23
